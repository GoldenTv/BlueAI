name: blue_app
description: "BlueAI mobile assistant."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.13.2

dependencies:
  flutter:
    sdk: flutter
  flutter_markdown: ^0.7.7+1
  flutter_markdown_latex: ^0.3.4
  flutter_math_fork: ^0.7.4
  http: ^1.6.0
  markdown: ^7.3.1
  shared_preferences: ^2.5.4
  flutter_secure_storage: ^10.3.1
  supabase_flutter: ^2.10.0
  uuid: ^4.5.1
  connectivity_plus: ^7.0.0
  google_sign_in: 7.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
